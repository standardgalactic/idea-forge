# python-research

Python research template with pytest, ruff linting, and project structure for computational research.

Generates:
- `src/<package>/` - Python package directory
- `tests/` - Pytest test suite
- `benchmarks/` - Performance benchmarks
- `pyproject.toml` - Modern Python packaging
- Ruff for linting and formatting
